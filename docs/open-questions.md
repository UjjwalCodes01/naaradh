# Open questions

Canonical list. `AGENTS.md §15` and `CLAUDE.md` both defer here. Reviewed weekly (cross-phase track in `PLAN.md`).

**Rule:** if a task depends on an open item, implement behind a flag with the conservative default and name the dependency in the PR. Never implement a guess.

Status values: `open` · `asked` (question sent, awaiting reply) · `closed` (answer on record + ADR written if it changed a decision).

| # | Question | Blocks | Owner | Status | Conservative default until closed |
|---|---|---|---|---|---|
| Q-01 | Which CLI series may a **non-BFSI** PE use for AI-driven **service** calls (COD confirmation)? 140 = promotional-only, 1600/1601 = BFSI/gov, 10-digit prohibited — sources conflict. | `numbers.purpose_allowed` defaults → ADR-0002 → Phase 4 promotional launch → any real Indian call | Founder + lawyer | open | `numbers.purpose_allowed` has **no default in code**; a human sets it per number. |
| Q-02 | Is DND/NCPR scrubbing required for **transactional** calls, or promotional only? | gate step 8 | Founder (TSP letters) | open | Flag `dnd.scrub_transactional = true` (scrub everything). |
| Q-03 | Is DCA (Digital Consent Acquisition) required for **voice** consent, or SMS only? | consent ledger sources | Founder + lawyer | open | Not implemented; ledger accepts `source='dca'` as a placeholder value only. |
| Q-04 | Per-second vs per-minute-rounded billing, and minimum billable duration, at Bolna / OmniDimension / Retell — **confirmed from an invoice or CDR, not docs**. | outcome pricing viability → Phase 0 exit / kill criteria | Founder | open | Assume per-minute rounding when modelling margin (the pessimistic case). |
| Q-05 | Who is **telemarketer-of-record** for TRAI complaints when dialling on a vendor's +91 numbers — vendor, Naaradh, or merchant? | complaint attribution logic, `complaints` worker | Founder | open | Attribute to Naaradh (assume worst case) and to the tenant for counters. |
| Q-06 | DPDP Rules final timelines for breach notification and erasure. `[LEGAL]` | retention/erasure constants | Lawyer | open | Constants in `packages/compliance/constants.ts` marked `TODO_LEGAL`; erasure target 30 days. |
| Q-07 | Shopify policy on an app updating customer marketing consent from a **verbal** opt-out. | `shopify.sync_optout` flag | Founder | open | Flag `shopify.sync_optout = false`. Suppress internally, do not write to Shopify. |
| Q-08 | Does Shopify permit voice-call consent wording on the SMS checkbox, or is a separate checkout-UI-extension checkbox required? `[LEGAL wording]` | Phase 4 abandoned cart (E-13) | Lawyer | open | Require the separate custom checkbox + ledger row. Never infer from `smsMarketingConsent`. |
| Q-09 | GoKwik / Shiprocket / Razorpay Magic / Cashfree abandoned-checkout webhook access — partner program or merchant-level keys? | Phase 5 abandoned-cart coverage (E-14) | Founder | open | No provider ingestion; those stores get COD confirmation only. |
| Q-10 | Shopify **Level 2** protected-customer-data review lead time in 2026. | Phase 3 App Store submission | Founder | open | Assume it is the critical path; submit the request in Phase 2, tolerate `null` phone (gate `no_phone`) until approved. |
| Q-11 | GST treatment of Shopify App Store payouts as export of services. `[CA]` | invoicing, LUT filing | CA | open | File LUT before the first export invoice. |
| Q-12 | Current two-party recording-consent US state list. | Phase 6 US scripts | Lawyer | open | Announce AI + recording on **every** US call regardless of state. |
| Q-13 | Is Hinglish quality on real Jio/Airtel/Vi 4G acceptable to customers? (Not a legal question — only the bake-off answers it.) | Phase 0 exit / kill criteria | Founder | open | Build the platform on the simulator; write **no vendor-specific adapter** until ADR-0001 records the bake-off result. |
| Q-14 | Does TRAI/DoT publish a **fictitious / drama mobile range** for India, like NANP's 555-01xx or Ofcom's 07700 900xxx? | Test fixtures (`packages/shared/test/fake-phones.ts`) | Founder | open | Repo uses `+91 6000 000 xxx` **by convention only** — it may be allocated to real subscribers. Protection is that dev/CI dial only through the simulator, which refuses any number failing `assertFakePhone()`. Never weaken that guard. |
| Q-15 | **Inbound + transfer in India:** (a) does an AI agent *answering* customer-initiated calls on a 10-digit virtual number carry any DLT/TCCCPR obligation? (b) when the AI transfers a live call to a merchant's manager, which CLI does the manager's leg show, and is that leg treated as a commercial call? Inbound is now the lead product (ADR-0006, PLAN Phase 1B). | Live inbound on a real Indian number; transfer-to-manager on both directions | Founder (add to the TSP letter) | open | Transfer only to merchant-configured, verified Indian mobile numbers; answer inbound with the same AI + recording disclosure as outbound. |
| Q-16 | Does an Indian merchant DPA, or Shopify's Level 2 review, require **in-country storage of call metadata** (hashes, encrypted numbers, outcomes)? The DB is on Neon in **Singapore** (ADR-0004); recordings/transcripts stay in GCS Mumbai. `[LEGAL]` | DPA wording, privacy policy, sub-processor list; possible migration to Cloud SQL Mumbai | Lawyer + founder | open | Name Neon + Singapore in the DPA and privacy policy now; keep every DB object vanilla-Postgres so a move is a `pg_dump`. |
| Q-17 | **Inbound pricing:** per-minute price book (SPEC §2.2) — ₹2,499 / 6,999 / 14,999 platform fee with 500 / 1,500 / 4,000 minutes, ₹6 / 5 / 4 overage — is a starting point. Confirm after bake-off cost data (Q-04) and pilot call length. `[DECISION — founder]` | Inbound billing, Shopify usage line, invoices | Founder | open | Meter per connected minute, rounded up per call; ledger rows carry minutes, price from plan config (`packages/pipeline/src/billing/plans.ts`). The USD column there (for merchants Shopify bills in dollars) is a placeholder at roughly ₹83/$ rounded — confirm with the INR prices. |
| Q-18 | How reliable is **caller ID** on Indian networks for inbound calls — how often withheld, how easily spoofed? Decides how much `caller_id` identity may unlock (ADR-0006). | Identity levels, E-83 | Founder (pilot data) | open | `caller_id` unlocks reading the caller's own orders and *requesting* cancellation of an unshipped COD order; never address, refund or prepaid cancellation. |
| Q-19 | **Address write-back.** Should Naaradh ever write a customer's changed delivery address to the Shopify order? The extraction captures it as free text ("flat 2, near the temple, new road"); Shopify's shipping address is structured (address1/2, city, province, zip). Writing needs a structured-address design (the agent collecting fields one by one + pincode lookup, or the merchant confirming a parsed suggestion) and a decision on who owns a misdelivery. Docs disagreed: P1-SHOP-2 said "no address write", the extraction schema "never auto-written", while `planWriteback()` would have written it when `address_write_enabled` and confidence ≥ 0.9. `[DECISION — founder]` | Address auto-write (outbound E-44), `tenants.address_write_enabled` | Founder | open | **Never write an address to Shopify**, whatever the tenant setting or confidence: tag `naaradh:address-review`, `writeback_status = needs_review`, suggested text in the dashboard only. Inbound address changes are always tickets (invariant 14). |
| Q-20 | **Stores outside India.** The Shopify app is public; a US or EU store can install it. Calling exists only in India (numbers, windows, DLT) and data lives in India/Singapore. Do we (a) block install, (b) waitlist (tenant created with its country's data region, no calling), or (c) serve them from India? GDPR/US law differ on where their merchants' data may sit. `[DECISION — founder]` `[LEGAL]` | apps/shopify onboarding, data residency (§6.1), App Store listing regions | Founder + lawyer | open | (b) Waitlist: the app installs, shows "coming to your region", creates no use cases' live state and never calls; the listing is limited to India if Shopify allows. |

## How to close one

1. Get the answer **in writing** from a primary source (TSP letter, vendor invoice, regulator text, lawyer's opinion) and file it — TSP replies in `docs/legal/tsp-responses/`, vendor answers alongside the bake-off sheet.
2. If the answer changes a decision, write an ADR in `docs/decisions/`.
3. Set the row to `closed` with a link to the evidence. Do not delete the row.
